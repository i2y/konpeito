# JVM: NullPointerException in test_ivar_and_assign_nil
# null.longValue() when @val is nil
# Process crashes, all tests fail
@value += n accumulates correctly
@value -= n subtracts correctly
@value *= n multiplies correctly
@data ||= assigns when nil
@data ||= does not overwrite existing value
@val &&= assigns when truthy
@val &&= does not assign when nil
@sum += i in loop: 0+1+2+3+4 = 10
