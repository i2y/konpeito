# multi_assign_spec: JVM runtime error (exit code 1)
# Cause: Multi-assignment or for loop desugaring fails in JVM backend
multi-assignment assigns first variable
multi-assignment assigns second variable
multi-assignment assigns first of three
multi-assignment assigns second of three
multi-assignment assigns third of three
multi-assignment with excess values assigns first correctly
multi-assignment with excess values assigns second correctly
multi-assignment with fewer values assigns first correctly
multi-assignment with fewer values assigns second correctly
multi-assignment with fewer values assigns nil to extra variable
multi-assignment works inside a method
for iterates over an Enumerable passing each element to the block
for executes code in containing variable scope
for breaks out of a loop upon break
for starts the next iteration with next
