# JVM: lambda arity/lambda? limitations — rb_proc_new creates Proc not Lambda
# Note: is_a?(Proc) fixed (RubyDispatch now checks isCallable for Proc class)
# Note: test_block_parameter fixed (generate_call_with_block skips block params)
# Note: test_lambda_returning_lambda passes via invokedynamic capture
