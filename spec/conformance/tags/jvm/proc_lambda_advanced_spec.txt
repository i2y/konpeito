# JVM: KBlock objects don't support .arity, .lambda?, .is_a?(Proc) methods
# Lambda returning lambda has capture semantics issues
# NoSuchMethodError on .arity crashes before reaching later tests
RUNTIME_ERROR
