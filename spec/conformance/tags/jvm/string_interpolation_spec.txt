# string_interpolation_spec: JVM runtime error
# Cause: String interpolation not fully implemented in JVM backend
gets interpolated with #{} when put in double quotes
calls #to_s when the object is not a String (Integer)
evaluates expressions inside #{}
interpolates multiple expressions in one string
permits an empty expression
evaluates method calls inside interpolation
calls #to_s for true
calls #to_s for false
calls #to_s for nil (returns empty string)
handles nested string operations in interpolation
calls #to_s for Float
