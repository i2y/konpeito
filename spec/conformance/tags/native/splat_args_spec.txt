# Native: TypeError — no implicit conversion of nil into Array
# sp_first_and_rest returns [first, rest] but rest is nil
# Process crashes, all tests fail
*args receives all arguments
*args with no arguments gives empty array
*args with single argument works
first param captures first arg
*rest captures remaining args
*rest first element
*rest second element
*rest third element
first param captures only arg
*rest is empty when only first arg given
*args count with no args
*args count with 3 args
*args count with 5 args
*array expands at call site
*args with strings works
greeting creates array of greetings
first greeting
second greeting
